- Run the following command to build the Docker images defined in your docker-compose.yml file:
   - docker-compose build
- After building the images, run the containers using the following command:
  - docker-compose up

- run the postman collection in code